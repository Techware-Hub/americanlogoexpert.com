import Link from "next/link";
import { ArrowRight, Mail, Phone } from "lucide-react";
import { contact,phoneDisplay } from "@/lib/contact";
export default function CTA(){return <section className="relative overflow-hidden bg-red-600 py-24"><div className="pointer-events-none absolute inset-0 grid-bg opacity-40"/><div className="container relative"><div className="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><div><span className="text-xs font-extrabold uppercase tracking-[.2em]">Ready when you are</span><h2 className="display mt-4 max-w-4xl text-[clamp(55px,8vw,105px)]">Let’s build a brand they can’t ignore.</h2></div><Link href="/contact" className="btn btn-light shrink-0">Start your project <ArrowRight size={17}/></Link></div><div className="mt-9 flex flex-col gap-4 border-t border-white/25 pt-6 text-sm font-semibold sm:flex-row sm:gap-8"><a href={`mailto:${contact.email}`} className="flex items-center gap-2 hover:text-black"><Mail size={17}/>{contact.email}</a><a href={`tel:${contact.phone}`} className="flex items-center gap-2 hover:text-black"><Phone size={17}/>{phoneDisplay}</a></div></div></section>}
