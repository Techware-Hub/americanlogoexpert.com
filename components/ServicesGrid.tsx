import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import { services } from "@/lib/data";
export default function ServicesGrid({limit}:{limit?:number}){return <div className="grid md:grid-cols-2 lg:grid-cols-3" data-aos="fade-up">{services.slice(0,limit).map(s=><article key={s.slug} className="premium-card group relative min-h-80 cursor-pointer border border-white/10 bg-[#0f1013] p-8 transition-all duration-300 ease-in-out hover:-translate-y-2 hover:scale-[1.03] hover:border-red-500 hover:shadow-2xl hover:shadow-red-500/30"><div className="flex justify-between"><s.icon size={34} strokeWidth={1.4} className="text-red-500 transition-transform duration-300 group-hover:scale-110 group-hover:-rotate-3"/><span className="display text-5xl text-white/[.06]">{s.num}</span></div><h3 className="display mt-12 text-3xl transition-colors group-hover:text-white">{s.title}</h3><p className="muted mt-4 text-sm">{s.short}</p><Link href={`/services/${s.slug}`} className="absolute bottom-7 left-8 flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-zinc-300 transition-all duration-300 group-hover:translate-x-1 group-hover:text-red-500">Learn more <ArrowUpRight size={15}/></Link></article>)}</div>}
